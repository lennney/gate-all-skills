---
name: gate-frontend
description: 前端开发阶段。项目搭好后加载。问用户要什么页面，你在后台建好。
---

用户的脚手架已经搭好了。问：

> "首页你想放什么内容？"

用户描述完，你在后台：
1. 设计页面结构（Hero + Features + Content + CTA）
2. 创建组件
3. Tailwind 样式
4. 填入内容
5. 确认 `npm run dev` 能跑

然后让用户预览。用户说"改这里" → 你改。说"再加一页" → 继续。

所有页面做完 → 引导去 `/gate-seo`。
